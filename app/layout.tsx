import "./globals.css";
export const metadata = { title: "DERBAAG Pre-order", description: "Gear for the trail" };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="th"><body>{children}</body></html>
}
